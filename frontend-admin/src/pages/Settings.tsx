import React from 'react';

const Settings: React.FC = () => {
  return (
    <div style={{ padding: '2rem' }}>
      <h1>Settings</h1>
      <p>Platform settings and configuration will be displayed here.</p>
    </div>
  );
};

export default Settings;