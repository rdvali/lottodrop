# Docker Environment Configuration for LottoDrop

# Database Configuration
DB_HOST=postgres
DB_PORT=5432
DB_NAME=lottodrop
DB_USER=lottodrop_user
DB_PASSWORD=secure_password_123

# Redis Configuration
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# Backend Configuration
BACKEND_PORT=3001
NODE_ENV=production
JWT_SECRET=your_jwt_secret_here_change_in_production
SESSION_SECRET=your_session_secret_here
BCRYPT_ROUNDS=12

# Frontend Configuration
FRONTEND_PORT=80
VITE_API_URL=http://localhost:3001/api
VITE_SOCKET_URL=http://localhost:3001
VITE_APP_NAME=LottoDrop
VITE_APP_VERSION=2.0.0

# Admin Panel Configuration
ADMIN_PORT=81
REACT_APP_API_URL=http://localhost:3001

# CORS Configuration
ALLOWED_ORIGINS=http://localhost,http://localhost:80,http://localhost:81,http://localhost:3000,http://localhost:3001

# Admin Credentials
ADMIN_EMAIL=admin@lottodrop.com
ADMIN_PASSWORD=Admin123!@#

# Database Migration Note
# The database has been migrated from local PostgreSQL to Docker
# Backup location: backend/migrations/lotto_drop_complete_backup.sql
# Migration date: 2025-09-15